# Frontend Environment Variables
# Copy this to .env.local for local development (optional)

# API Base URL (optional)
# Defaults to /api in production, http://localhost:5000/api in development
# Only set this if you need to override the default
# VITE_API_URL=http://localhost:5000/api

# Supabase (optional - only if using Supabase features)
# VITE_SUPABASE_URL=your-supabase-url
# VITE_SUPABASE_PUBLISHABLE_KEY=your-supabase-key

