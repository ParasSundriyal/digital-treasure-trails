# Dependencies (these are installed, not needed in deployment)
node_modules
server/node_modules

# Build outputs (generated during build)
dist
build
server/dist

# Environment files (sensitive, should not be deployed)
.env
.env.local
.env.*.local
server/.env
server/.env.local

# Logs and temp files
*.log
.DS_Store

# Documentation files (not needed for runtime)
*.md
!README.md

# Note: 
# - server/src is NEEDED for the API handler (api/[...path].ts imports from it)
# - server/package.json might be needed for Vercel's build process
# - These files are NOT served as static files due to vercel.json routing rules
# - Only /api/* routes go to the serverless function, everything else goes to frontend
